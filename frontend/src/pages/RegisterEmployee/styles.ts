import { SystemStyleObject } from '@chakra-ui/react';

export const RegisterEmployeeStyles: Record<string, SystemStyleObject> = {
  formContainer: {
    width: '100%',
    height: 'calc(72rem + 4rem)',
    alignItems: 'center',
    justifyContent: 'center',
    position: 'relative',
  },
};
